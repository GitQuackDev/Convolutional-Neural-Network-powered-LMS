# AI Services Environment Configuration
# Copy this file to .env in your backend/ directory

# =================================================================
# RECOMMENDED: OpenRouter (Single API for 499+ models)
# Get your API key from: https://openrouter.ai/keys
# =================================================================
OPENROUTER_API_KEY=sk-or-your-openrouter-api-key-here
# Gives access to GPT-4, Claude, Gemini, and 496+ other models
# Many models are FREE (DeepSeek V3.1, Gemini 2.5 Flash)
# Much cheaper than individual APIs

# =================================================================
# ALTERNATIVE: Individual APIs (More Expensive)
# =================================================================

# =================================================================
# OpenAI Configuration (GPT-4)
# Get your API key from: https://platform.openai.com/api-keys
# =================================================================
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_ENDPOINT=https://api.openai.com/v1
OPENAI_MAX_RETRIES=3
OPENAI_TIMEOUT=30000
OPENAI_MAX_CONTENT_LENGTH=50000
OPENAI_REQUESTS_PER_MINUTE=60
OPENAI_REQUESTS_PER_HOUR=3600
OPENAI_FAILURE_THRESHOLD=5
OPENAI_RESET_TIMEOUT=60000
OPENAI_MONITORING_PERIOD=60000

# =================================================================
# Anthropic Configuration (Claude)
# Get your API key from: https://console.anthropic.com/
# =================================================================
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ANTHROPIC_ENDPOINT=https://api.anthropic.com
ANTHROPIC_MAX_RETRIES=3
ANTHROPIC_TIMEOUT=30000
ANTHROPIC_MAX_CONTENT_LENGTH=100000
ANTHROPIC_REQUESTS_PER_MINUTE=50
ANTHROPIC_REQUESTS_PER_HOUR=3000
ANTHROPIC_FAILURE_THRESHOLD=5
ANTHROPIC_RESET_TIMEOUT=60000
ANTHROPIC_MONITORING_PERIOD=60000

# =================================================================
# Google AI Configuration (Gemini)
# Get your API key from: https://makersuite.google.com/app/apikey
# =================================================================
GOOGLE_AI_API_KEY=your_google_ai_api_key_here
GOOGLE_AI_ENDPOINT=https://generativelanguage.googleapis.com
GOOGLE_AI_MAX_RETRIES=3
GOOGLE_AI_TIMEOUT=30000
GOOGLE_AI_MAX_CONTENT_LENGTH=30000
GOOGLE_AI_REQUESTS_PER_MINUTE=60
GOOGLE_AI_REQUESTS_PER_HOUR=1500
GOOGLE_AI_FAILURE_THRESHOLD=5
GOOGLE_AI_RESET_TIMEOUT=60000
GOOGLE_AI_MONITORING_PERIOD=60000

# =================================================================
# Service Management Configuration
# =================================================================
# Comma-separated list of enabled services (only services with API keys will actually work)
AI_ENABLED_SERVICES=gpt4,claude,gemini

# Default service to use if no preference is specified
AI_DEFAULT_SERVICE=gpt4

# Fallback order if the primary service fails
AI_FALLBACK_ORDER=gpt4,claude,gemini

# =================================================================
# Security & Performance Notes:
# - Keep your .env file in .gitignore
# - Use different API keys for development/staging/production
# - Monitor your usage to avoid unexpected costs
# - Set up billing alerts on your AI service accounts
# =================================================================
